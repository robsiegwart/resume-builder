[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resumepy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0",
    "jinja2>=3.0",
    "pdfkit>=0.6.1",
    "pyyaml>=6.0",
]

[project.scripts]
resumepy = "resumepy.cli:cli"
